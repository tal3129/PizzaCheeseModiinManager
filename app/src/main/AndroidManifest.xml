<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="com.pizzacheese.pizzacheesemanager">

	<uses-permission android:name="android.permission.CAMERA"/>
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
	<uses-permission android:name="android.permission.INTERNET"/>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="false"
		android:theme="@style/AppTheme">
		<activity android:name=".MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".EditProducts"/>
		<activity
			android:name=".AddProduct"
			android:theme="@style/NoTitleDialog"/>

		<activity
			android:name=".DeleteProduct"
			android:theme="@style/NoTitleDialog"/>

		<activity android:name=".EditSettings"/>
		<activity android:name=".EditDiscounts"/>
		<activity
			android:name=".addDiscount"
			android:theme="@style/NoTitleDialog"/>
		<activity android:name=".EarningsActivity"/>
		<activity android:name=".PrefProductsActivity"/>
		<activity android:name=".OrdersActivity"/>
		<activity android:name=".EditLocations">
		</activity>
	</application>

</manifest>


